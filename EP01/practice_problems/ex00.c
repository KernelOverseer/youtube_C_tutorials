#include <stdio.h>

/*
** Salam, hada exercice lewel :
** Fach ghatcompili lprogram ou texecutih, ghadi igoulik dekhel wa7d le nombre
** hadak le nombre kayt7ett f variable num, ila kan dak number pair, i3ni men
** lmoda3afat dial jouj, ghatkteb "PAIR", ila ma kanch ghatkteb "IMPAIR"
*/

int main(void)
{
	int number;

	//hna bach kantleb men utilisateur idekhel number
	printf("Enter a number : ");
	scanf("%d", &number);

	// Bda lcode dialek men hna, khdem b printf


	
	return (0);
}

// Hint 1: khdem b if else
// Hint 2: khdem b modulo 2 example if (num % 2 == 0)
