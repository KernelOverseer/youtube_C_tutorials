#include <stdio.h>

/*
** Hada exercice tani :
** Had lprogram kaytleb men l'user idekhel number a ou number b
** ila kan a kber men b, kteb "a", ila kan b kber men a, kteb "b"
*/

int main(void)
{
	int a;
	int b;

	//hna bach kantleb men utilisateur idekhel numbers
	printf("Enter number (a): ");
	scanf("%d", &a);
	printf("Enter number (b): ");
	scanf("%d", &b);

	// Bda lcode dialek men hna, khdem b printf


	
	return (0);
}

// Hint 1: khdem b if else
// Hint 2: khdem b comparison >
